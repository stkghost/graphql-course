import { RequestProcessContext } from './types';
export declare const processRequest: <TContext, TRootValue = {}>({ contextFactory, execute, operationName, parse, query, request, schema, subscribe, validate, variables, extraHeaders, }: RequestProcessContext<TContext, TRootValue>) => Promise<Response>;
